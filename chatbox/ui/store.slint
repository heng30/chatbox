export struct ChatItem {
    utext: string,
    btext: string,
    etext: string,
}

export struct ChatSession {
    name: string,
    uuid: string,
}

export global Store  {
    in-out property<[ChatItem]> session-datas: [
        /* { utext: "show me some rust code.", btext: "int main() {return 0;}", etext:""}, */
        /* { utext: "This is not rust code.",  btext: "function main(){}", etext:""}, */
    ];

    in-out property<[ChatSession]> chat-sessions: [ ];
    in-out property<string> input-text;
}

