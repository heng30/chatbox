export struct ChatItem {
    role: string,
    text: string,
}

export struct ChatSession {
    name: string,
    uuid: string,
}

export global Store  {
    in-out property<[ChatItem]> session-datas: [
        /* { text: "Blue", role: "user"}, */
        /* { text: "Red",  role: "bot"}, */
        /* { text: "Green", role: "user"}, */
        /* { text: "Yellow", role: "bot"}, */
    ];

    in-out property<string> chat-current-prompt: chat-prompts[0];
    in-out property<[string]> chat-prompts: [
        "Serve me as a chat bot.",
        "Serve me as a writing and programming assistant.",
    ];

    in-out property<[ChatSession]> chat-sessions: [ ];
    in-out property<string> input-text;
}

