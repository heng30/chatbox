import { Theme } from "../theme.slint";
import { Divider } from "../base/divider.slint";
import { Header } from "./header.slint";
import { Body } from "./body.slint";
import { Bottom } from "./bottom.slint";

export component LPanel inherits Rectangle {
    width: Theme.left-panel-width;
    background: Theme.dark-fill-color;

    animate width {
        duration: Theme.default-animate-duration;
        easing: ease-in-out;
    }

    VerticalLayout {
        Header {
            visible: Theme.left-panel-width > 0;
        }

        Divider {
            x: Theme.padding;
            width: root.width - Theme.padding * 2;
            background: Theme.regular-text-color;
        }

        Body { }
    }
}

